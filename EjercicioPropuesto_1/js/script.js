function guardar(n,p) {
    let Nom = [];
    let Pues = [];

    let nomLista = Nom.push(n);
    let puesLista = Nom.push(p);
    
    console.log(Nom.length)
    console.log(Pues.length)
}

function calcular(h1) {
    var hora_total = h1 * 20;
    return (hora_total * 5.8125);
}

var sueldo_mensual = calcular(horas);
var registro = guardar(nombre,puesto);